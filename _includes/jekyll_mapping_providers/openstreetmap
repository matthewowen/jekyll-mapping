<script src="http://www.openlayers.org/api/OpenLayers.js"></script>
<script type="text/javascript">
var map;
jekyllMapping = function(){
    return {
        mappingInitialize: function() {
            var markers = new OpenLayers.Layer.Markers("Markers")
            var center = new OpenLayers.LonLat({{ page.longitude }},{{ page.latitude }}).transform( new OpenLayers.Projection("EPSG:4326"), new OpenLayers.Projection("EPSG:900913");
            var map = new OpenLayers.Map("jekyll-mapping");
            map.addLayer(new OpenLayers.Layer.OSM());
            map.setCenter(center, {{ site.mapping.zoom }});
            map.addLayer(markers);
            markers.addMarker(new OpenLayers.Marker(center));
        }
    }
}()

jekyllMapping.mappingInitialize();
</script>